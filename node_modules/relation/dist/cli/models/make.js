'use strict';

Object.defineProperty(exports, "__esModule", {
  value: true
});

exports.default = function (_ref) {
  var args = _ref.args,
      cwd = _ref.cwd,
      fs = _ref.fs;

  var filePath = cwd + '/models/' + args[0].toLowerCase() + '.js';

  var template = 'import { Model } from \'relation\'\n\nexport default class ' + args[0] + ' extends Model {\n\n}\n';

  try {
    fs.accessSync(cwd + '/models', fs.F_OK);
  } catch (e) {
    fs.mkdirSync(cwd + '/models');
  }

  fs.writeFile(filePath, template, function (err) {
    if (err) throw err;
  });

  console.log('Created model: ', filePath);
};