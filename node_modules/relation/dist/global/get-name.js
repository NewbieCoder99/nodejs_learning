'use strict';

var _pluralize = require('pluralize');

var _pluralize2 = _interopRequireDefault(_pluralize);

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

module.exports = {
  getTableName: function getTableName(model) {
    return typeof model === 'string' ? (0, _pluralize2.default)(model).toLowerCase() : null;
  },
  getFieldName: function getFieldName(model) {
    return typeof model === 'string' ? model.toLowerCase() + '_id' : null;
  }
};